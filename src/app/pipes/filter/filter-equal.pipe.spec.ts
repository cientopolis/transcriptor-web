import { FilterEqualPipe } from './filter-equal.pipe';

describe('FilterEqualPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEqualPipe();
    expect(pipe).toBeTruthy();
  });
});
