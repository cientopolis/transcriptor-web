export const environment = {
  production: true,
  apiUrl: 'localhost:8080',
  transcribe: {
    classicMode: false,
    layers: true
  },
  semantic_transcription: {
    prefix: "http://transcriptor-dev.com/"
  },
  usePusher: false
};
